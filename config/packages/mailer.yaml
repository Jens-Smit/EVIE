framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: '%env(default::MAILER_FROM)%'

parameters:
    mailer_from: '%env(default::MAILER_FROM)%'

when@test:
    framework:
        mailer:
            dsn: 'null://null'
    parameters:
        mailer_from: 'no-reply@evie.test'
